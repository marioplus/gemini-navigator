{
  "name": "gemini-navigator",
  "private": true,
  "version": "0.1.3",
  "description": "Gemini 对话导航",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "version:patch": "npm version patch && git push origin master --tags",
    "version:minor": "npm version minor && git push origin master --tags",
    "version:major": "npm version major && git push origin master --tags",
    "version": "node scripts/update-changelog.js && git add CHANGELOG.md"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vite-plugin-monkey": "^7.1.1"
  }
}
